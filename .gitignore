# Dependencies
node_modules/

# Build output
dist/
production/

# Incremental-deploy state + staging (see scripts/deploy-diff.mjs)
.deploy/
production-upload/

# Environment
.env
.env.local
.env.*.local

# Editor / OS
.DS_Store
.vscode/
.idea/
*.swp
*.swo
*~

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Cache
.eslintcache
.vite/

# Video originals (uncompressed backups)
public/assets/originals/

# Uncompressed master videos / source exports (backups, not shipped)
video-masters/

# Playwright failure artifacts (NOT baselines — *-snapshots/ is committed)
test-results/
playwright-report/

# Local Claude Code config (per-developer, not shared)
CLAUDE.md
.mcp.json
.claude/

# dev-process-toolkit artifacts — used locally by us only, not part of the
# client deliverable (specs/FRs/plan live outside the pushed repo)
specs/
