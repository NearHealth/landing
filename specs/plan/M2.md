---
milestone: M2
status: active
archived_at: null
kickoff_branch: null
frozen_at: null
---

# Implementation Plan

## Milestone Order

M2 depends on M1 (scaffolding). It delivers the reusable contact-form component.

## M2: Reusable contact form {#M2}

**Goal:** Ship a reusable, atom-composed contact form (`ContactForm` + `ui/form/` atoms),
upgraded to the new Figma design + checklist, and rebuild `ContactPage` on top of it.
**Prerequisites:** M1

**FR list:**

| FR | Title | Tracker |
|----|-------|---------|
| KKADA9 | Reusable contact form component | n/a (mode: none) |

**Tasks:**
- [x] Build presentational atoms in `src/components/ui/form/` (TextField, SelectField, TextareaField, SubmitButton, LegalNote, FieldError)
  verify: atoms render in isolation; `npm run build:spa` passes
- [x] Build the `ContactForm` container (state, validation, Org→Partnership dependency, submit lifecycle, honeypot) with an `onSubmit` prop
  verify: Playwright validation + dependency + submit-lifecycle specs pass
- [x] Style to the Figma frames (glass card, fields, cyan submit) with 2-col desktop/tablet, 1-col mobile
  verify: visual check at desktop/tablet/mobile widths matches Figma (sans the deliberate breaks)
- [x] Rebuild `ContactPage` to render `<ContactForm/>` with the intent-driven hero; replace the `setTimeout` simulation with the `onSubmit` prop
  verify: `/contact?intent=early-access` and `?intent=talk` render correct hero + submit label; `npm run build:spa` passes

**Tests:**
- Playwright: validation (empty + bad email), Org→Partnership dependency, loading/double-submit,
  success swap, reject → system-error, honeypot drop, responsive layout.

**Acceptance Criteria:**
- [x] AC-KKADA9.1 … AC-KKADA9.13 (see `specs/frs/KKADA9.md`)

**Gate:** `npm run build:spa`
